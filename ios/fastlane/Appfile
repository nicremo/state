app_identifier("com.fabincrm.state")

# Account specific values live in ios/fastlane/.env, which is gitignored.
# See .env.example. This repository is public and carries nobody's account.
apple_id(ENV["APPLE_ID"])
itc_team_id(ENV["APP_STORE_CONNECT_TEAM_ID"])
team_id(ENV.fetch("APPLE_TEAM_ID", "5DKU7FFK4X"))
